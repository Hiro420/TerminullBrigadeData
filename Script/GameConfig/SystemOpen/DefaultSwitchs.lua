--配置方式：["ID"] = false, ID是定义在SystemOpenID.lua中的
-- 策划需要重点注意这里的双引号都是英文半角的双引号
local DefaultSwitchs = {
    --示例：默认关闭充值, falise表示默认关闭，true表示默认开启
    --["purchase"] = false,
    --示例：默认关闭权益
    --["monthcard"] = false,
}

return DefaultSwitchs